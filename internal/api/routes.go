package api

import (
	"github.com/gin-gonic/gin"
)

func AttachRoutes(router *gin.Engine) {
	//router.GET("/user/:phone/balance", Something)
	//router.GET("/user/statistics", Something)
	//router.POST("/user/increase", Something)
}
